"""Observer server internals (SYS-C2 split).

Submodules host the implementation behind ``werewolf_eval.observer_server``,
which remains the public facade — import from there, not from here, unless you
are inside the observer package itself.
"""
