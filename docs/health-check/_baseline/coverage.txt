coverage UNAVAILABLE on this host (best-effort attempt failed).

Reason: `python -m pip install coverage` could not reach PyPI — the local
network routes pip through a proxy that times out (ProxyError 'Cannot connect
to proxy' / TimeoutError), consistent with the documented env limit
(localhost HTTP blocked / GitHub+PyPI egress unreliable from the agent shell).

Consequence for this health check:
- No per-module line-coverage numbers were produced locally.
- Phase 2 (slimming) therefore did NOT use coverage=0% as a dead-code signal;
  it relied on the stdlib baseline signals instead: import-refs.txt,
  entrypoint-wiring.txt, doc-refs.txt, bigfiles.txt, plus git grep for
  dynamic references.
- Phase 3 (testability) flagged the missing coverage instrumentation itself
  as opportunity T-1 (see 03-architecture-optimization.md).

Where to get the real signal: run coverage in CI (Linux), e.g.
  PYTHONPATH=src python -m coverage run -m unittest discover -s tests -p "test_*.py"
  python -m coverage report -m
