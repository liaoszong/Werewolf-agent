# oh-my-harness:begin
!.agents/
!.agents/skills/
!.agents/skills/**
!.codex/
!.codex/hooks/
!.codex/hooks.json
!.codex/hooks/tree.mjs
!.oh-my-harness/
!.oh-my-harness/tree.md
# oh-my-harness:end

__pycache__/
*.pyc
.tmp/
# local observer run output (game-log/snapshots/events) — never committed
.runs/
# local + server-seeded match profiles (the default is regenerated on startup by
# observer_server._seed_default_profile; user profiles are local data)
profiles/

# Generated context-budget artifacts
docs/generated-context/*.index.json
docs/generated-context/current-task.ctx.md
.logs/validate/
.logs/review/
# Transient brainstorming scratch (move to docs/ if a note becomes real doc)
.grill/

# API keys — never commit
docs/secrets/api-keys.md

# Qt observer local files
clients/qt_observer/.qtcreator/
clients/qt_observer/*.user
clients/qt_observer/*.user.*
clients/qt_observer/build/

.worktrees/

.omo/

# brainstorming visual companion mockups
.superpowers/

# Claude Code agent worktree scratch (embedded git repos)
.claude/worktrees/

# Release virtualenv (isolated from dev — never committed)
.venv-release/

.worktrees/
